[phases.setup]
nixPkgs = ["python311", "tesseract-ocr", "libtesseract-dev"]

[phases.install]
cmds = ["python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app"